pyyaml  # Для загрузки конфигурационных файлов

# Инструменты для pre-commit hooks (устанавливаются автоматически при необходимости):
ruff>=0.1.0  # Линтер для Python
black>=23.0.0  # Форматирование кода
isort>=5.12.0  # Сортировка импортов
pytest>=7.0.0  # Тестирование